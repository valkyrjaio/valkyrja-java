name: Create a New Release

on:
  workflow_dispatch:
    inputs:
      bump:
        description: 'Version component to bump'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major
          - rc

run-name: "Release ${{ inputs.bump }} bump on ${{ github.ref_name }}"

permissions:
  contents: write

jobs:
  release:
    uses: valkyrjaio/.github/.github/workflows/_java-create-release.yml@716065aae4466b95f0f80c9c5a5e2a92de620af6
    secrets: inherit
    with:
      bump: ${{ inputs.bump }}
      dependencies: |
        [
          {"name": "Spotless",    "directory": ".github/ci/spotless"},
          {"name": "ArchUnit",    "directory": ".github/ci/archunit"},
          {"name": "ErrorProne",  "directory": ".github/ci/errorprone"},
          {"name": "SpotBugs",   "directory": ".github/ci/spotbugs"},
          {"name": "JUnit",      "directory": ".github/ci/junit"}
        ]
      info-class-path: src/main/java/io/valkyrja/application/constant/ApplicationInfo.java
      info-class-name: ApplicationInfo

  publish:
    needs: release
    uses: valkyrjaio/.github/.github/workflows/_java-release-maven-publish.yml@716065aae4466b95f0f80c9c5a5e2a92de620af6
    secrets: inherit
